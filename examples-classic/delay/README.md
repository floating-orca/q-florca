Illustrates how we can delay execution of functions in various ways with a simple, custom plugin function `delay`.

The `delay` function will just wait for a second before forwarding its input to whatever function has been passed to the `delay`'s `fn` param.

See `start.ts` for an example of how flexible such a simple wrapper function can be.
